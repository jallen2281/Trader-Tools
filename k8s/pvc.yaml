apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trader-tools-data
  namespace: trader-tools
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard  # Adjust based on your cluster
