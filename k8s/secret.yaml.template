# Create the actual secret using:
# kubectl create secret generic trader-tools-secret \
#   --from-literal=SECRET_KEY='your-secret-key-here' \
#   --from-literal=DATABASE_URL='postgresql://user:password@supabase-host:5432/postgres' \
#   --from-literal=GOOGLE_CLIENT_ID='your-google-client-id' \
#   --from-literal=GOOGLE_CLIENT_SECRET='your-google-client-secret' \
#   --from-literal=ALPHA_VANTAGE_API_KEY='your-alpha-vantage-key' \
#   --from-literal=NEWS_API_KEY='your-news-api-key' \
#   --from-literal=QUANDL_API_KEY='your-quandl-key' \
#   --from-literal=PLAID_CLIENT_ID='your-plaid-client-id' \
#   --from-literal=PLAID_SECRET='your-plaid-secret' \
#   --from-literal=PLAID_ENCRYPTION_KEY='fernet-key-see-below' \
#   --namespace trader-tools

# Or use this template (remember to base64 encode values):
apiVersion: v1
kind: Secret
metadata:
  name: trader-tools-secret
  namespace: trader-tools
type: Opaque
data:
  # All values must be base64 encoded
  # Example: echo -n 'your-secret-key' | base64
  SECRET_KEY: ""  # REQUIRED: Flask secret key
  DATABASE_URL: ""  # REQUIRED: postgresql://user:password@host:5432/postgres
  GOOGLE_CLIENT_ID: ""  # REQUIRED for OAuth
  GOOGLE_CLIENT_SECRET: ""  # REQUIRED for OAuth
  ALPHA_VANTAGE_API_KEY: ""  # Optional
  NEWS_API_KEY: ""  # Optional
  QUANDL_API_KEY: ""  # Optional
  ANTHROPIC_API_KEY: ""  # Optional — enables Claude for AI portfolio reads; falls back to local LLM if empty
  PLAID_CLIENT_ID: ""  # Optional — enables bank connections
  PLAID_SECRET: ""
  # Fernet key encrypting stored Plaid access tokens. Required to connect a bank.
  #   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
  PLAID_ENCRYPTION_KEY: ""
  OLLAMA_BASE_URL: ""  # Default: http://localhost:11434
