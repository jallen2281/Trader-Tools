apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: trader-tools

resources:
  - namespace.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

# Images to use (update with your registry)
images:
  - name: your-registry/trader-tools
    newName: your-registry/trader-tools
    newTag: latest

# Common labels
commonLabels:
  app.kubernetes.io/name: trader-tools
  app.kubernetes.io/instance: production
  app.kubernetes.io/version: "1.0.0"
  app.kubernetes.io/managed-by: kustomize

# Namespace for all resources
# namespace: trader-tools

# Config for generating resources
configMapGenerator: []
secretGenerator: []

# Replicas (can be overridden)
replicas:
  - name: trader-tools
    count: 2
